<h3>
  My Orders:
</h3>

<% if @orders.count == 0 %>
  None at the moment.
<% else %>

  <% @orders.each do |order| %>
    <div class="well transparentBackground">
    <% order.ordered_products.each do |ordered_product| %>
      <div>
        <p>
          <strong><%= ordered_product.product.name %></strong>
          Qty: <%= ordered_product.qty || 0 %> </p>
      </div>
    <% end %>
    </div>
  <% end %>
<% end %>